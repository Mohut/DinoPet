using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DinosaurManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Dinosaur dinosaur = new Dinosaur("Max", true, false, true);
    }

}
